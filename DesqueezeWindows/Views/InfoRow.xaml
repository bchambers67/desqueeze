<UserControl x:Class="DesqueezeWindows.InfoRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DesqueezeWindows">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="LabelBlock"
                   Grid.Column="0"
                   FontFamily="{StaticResource GroteskFont}"
                   FontSize="9"
                   local:Track.Em="0.12"
                   Foreground="{StaticResource GrainBrush}"
                   VerticalAlignment="Center"/>
        <TextBlock x:Name="ValueBlock"
                   Grid.Column="1"
                   FontFamily="{StaticResource MonoFont}"
                   FontSize="{StaticResource FontSizeMono}"
                   Foreground="{StaticResource HalideBrush}"
                   VerticalAlignment="Center"/>
    </Grid>
</UserControl>
